<%= will_paginate @comments %>
<% @comments.each do |comment| %>
	<div class="comment">
		<%= link_to avatar_thumb(comment.user), comment.user %>
		<%= comment.message %>
		<div class="comment_details"><%= comment.created_at.localtime.strftime("%b %e, %Y %l:%M %p %Z") %></div>
	</div>
<% end %>