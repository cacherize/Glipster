<div class="center">
	<%= direct_upload_form_for @uploader, html: {class: 'form'} do |f| %>
		<h2 class="form_header">Select File</h2>
		<div class="form_field">
			<%= f.file_field :flash_file %>
		</div>
		<div class="form_submit">
			<%= f.submit %>
		</div>
	<% end %>
</div>